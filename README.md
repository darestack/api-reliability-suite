# API Reliability & Debugging Suite

 ![Thumbnail](docs/thumbnail.png)

 ![CI Pipeline](https://github.com/daretechie/api-reliability-suite/actions/workflows/ci.yml/badge.svg)
 ![Version](https://img.shields.io/badge/version-1.0.0-gold)
 ![Architecture](https://img.shields.io/badge/architecture-Hexagonal-orange)
 ![Python](https://img.shields.io/badge/python-3.12%20%7C%203.13-blue)
 ![FastAPI](https://img.shields.io/badge/FastAPI-0.128-009688)
 [![GitHub Sponsors](https://img.shields.io/badge/Sponsor-%E2%9D%A4-pink)](https://github.com/sponsors/daretechie)

 ---

-## 🏛️ Best-in-Class Architecture
-
-This project follows **Hexagonal Architecture (Ports & Adapters)** to decouple business logic from infrastructure.
-
-```mermaid
-graph TD
-    subgraph "External World"
-        Client[Client Request]
-        Jaeger[Jaeger Tracing]
-        Prom[Prometheus]
-    end
-
-    subgraph "Infrastructure Adapters (Driving)"
-        API["FastAPI Entrypoint<br/>(src.main)"]
-    end
-
-    subgraph "Core Application (The Domain)"
-        direction TB
-        Service["Auth Service<br/>(src.services.auth_service)"]
-        Domain["Domain Models<br/>(src.domain.models)"]
-    end
-
-    subgraph "Infrastructure Adapters (Driven)"
-        Repo["User Repository<br/>(src.infrastructure.user_repository)"]
-        LLM["AI Adapter<br/>(src.core.llm)"]
-        Logger["Structlog Adapter<br/>(src.core.logging)"]
-    end
-
-    subgraph "Observability Sidecars"
-        OTel[OpenTelemetry Collector]
-        Metrics[Metrics Instrumentator]
-    end
-
-    Client --> API
-    API --> Service
-    Service --> Domain
-    Service --> Repo
-
-    API -.-> OTel
-    API -.-> Metrics
-    LLM -.-> API
-
-    OTel --> Jaeger
-    Metrics --> Prom
-```
-
-### Why this wins:
-1.  **Testability**: The core domain can be tested without databases or external APIs.
-2.  **Strictness**: Every model uses **Pydantic Strict Mode**—no accidental type casting.
-3.  **Traceability**: Every request is tagged with a **Correlation ID** and distributed **Trace ID**.
-
----
-
-## 🏗️ Must-Have Features (2026 Aligned)
-
-### 1. Robust Design Patterns
-*   **Hexagonal Isolation**: Business logic never touches `FastAPI` objects directly.
-  - **Correlation IDs**: Middleware-injected IDs for end-to-end log stitching.
-  - **Circuit Breakers**: `pybreaker` protects the system from 3rd party API hangs.
-
-### 2. The Observability Stack
-*   **Multidimensional Tracing**: Logs connect automatically to OpenTelemetry spans.
-*   **Structured JSON Logs**: `structlog` formatted for ELK/Datadog ingest.
-*   **RED Metrics**: Automated Rate, Error, and Duration tracking via Prometheus.
-
-### 3. AI-Powered Triage
-*   **Self-Healing Prep**: LLM adapter (`AsyncGroq`/`AsyncOpenAI`) summarizes 500 errors and suggests **JSON-formatted remediations**.
-
----
-
-## 🚀 Quick Start
-
-```bash
-# Install dependencies
-make install
-
-# Run the full stack (API + Metrics + Tracing)
-docker compose up -d
-
-# Run tests with coverage
-make test
-```
-
----
-
-## 📡 Endpoints Overview
-
-| Endpoint | Description | Pattern |
-|----------|-------------|---------|
-| `GET /health` | Health Check | Response Modeling |
-| `POST /login` | JWT Authentication | Service-Oriented |
-| `GET /debug/summarize-errors` | AI Log Triage | External Adapter |
-| `GET /external-api` | Circuit Breaker Demo | pybreaker Proxy |
-| `GET /metrics` | Prometheus Export | Pull-based Obs |
-
----
-
-## 👷 Developer Tools
-
-```bash
-make lint    # Ruff-powered code quality
-make format  # Consistent style enforcement
-make clean   # Environment cleanup
-```
-
----
-
-## 🤝 Hire Me
-
-Looking for a **Reliability Engineer** or **Backend Architect**?
-📧 [adelekedare2012@gmail.com](mailto:adelekedare2012@gmail.com) | [LinkedIn](https://linkedin.com/in/daretechie)
